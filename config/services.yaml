# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
#
# Fallback values used when the corresponding env var is not set (no .env is committed;
# real deployments provide these as actual environment variables).
parameters:
    env(APP_SECRET): ''
    env(REDIS_URL): 'redis://localhost:6379'
    env(LOCK_DSN): 'flock'
    env(CORS_ALLOW_ORIGIN): '^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'
    env(DEFAULT_URI): 'http://localhost'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    app.rate_limiter.storage:
        class: Symfony\Component\RateLimiter\Storage\CacheStorage
        arguments:
            - '@cache.rate_limiter'

    # Webhook URLs come from unauthenticated callers, so the client must not be
    # usable as a relay: redirects are refused (they would otherwise sidestep the
    # URL check) and private/reserved ranges are blocked after DNS resolution.
    app.http_client.webhook.transport:
        class: Symfony\Contracts\HttpClient\HttpClientInterface
        factory: ['Symfony\Component\HttpClient\HttpClient', 'create']
        arguments:
            - { max_redirects: 0, timeout: 5, max_duration: 10 }

    app.http_client.webhook:
        class: Symfony\Component\HttpClient\NoPrivateNetworkHttpClient
        arguments:
            - '@app.http_client.webhook.transport'

    App\Service\Alert\WebhookNotifier:
        arguments:
            $client: '@app.http_client.webhook'

    Predis\ClientInterface:
        class: Predis\Client
        arguments:
            - '%env(REDIS_URL)%'
